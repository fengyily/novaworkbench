{"subtasks":[
  {
    "title": "前端：ModelSelect 改为两层联动下拉（先选配置类型，再选模型）",
    "prompt": "在 /Users/f1/.novaworkbench/worktrees/novaworkbench/req_13f7c24e1d3a32fe 工作目录下完成。\n\n## 背景\nModelSelect.tsx 是 wizard pipeline 三个阶段（analyst/architect/developer）共用的模型选择组件，被 RequirementDetail.tsx:1565（开始开发弹窗的「开发模型」字段）等处使用。当前实现调 claudeApi.active() 只取激活配置的 models，UI 是单层 <select>。\n\n后端 wizard.go 的 analyst/architect/developer-chat / start-coding / adjust-coding / continue-coding 全部接收 req.Model string 作为阶段覆盖，无 ModelInActiveList 校验；requirements.{analyst_model,architect_model,developer_model} TEXT 列已存在（schema.go:398-400）。**后端无需改动**，前端把 model 字符串透传即可。\n\n## 目标\n把 ModelSelect 从「仅激活配置 models 的单层 select」改为「先选配置（默认激活），再选该配置下的 models —— 未激活配置的 models 也可选」的两层联动下拉。一次改动覆盖三个阶段（保持体验一致），**props 接口不变**（value: string + onChange: (model: string) => void），不引入 config_id 字段。\n\n## 具体改动（仅 ModelSelect.tsx）\n\n### 1. 数据源替换\n- 删 useEffect 中的 `claudeApi.active()` 调用。\n- 改为 useEffect 调 `claudeApi.list()` 拿 `ClaudeConfigItem[]`，存到 `configs` state。\n- 同步算出 `activeConfig = configs.find(c => c.is_active)` 与 `defaultConfigId = activeConfig?.id ?? configs[0]?.id ?? ''`。\n\n### 2. 新增 state\n- `const [configs, setConfigs] = useState<ClaudeConfigItem[]>([]);`\n- `const [selectedConfigId, setSelectedConfigId] = useState<string>('');` —— 在 list 返回后用 `useEffect` 设置为 `defaultConfigId`。\n- 保留原 `models: string[]` 的派生计算：从 `configs.find(c => c.id === selectedConfigId)?.models.map(m => m.model)` 算出当前可见 models。\n\n### 3. defaultModelName 解析保持\n- `defaultModelName` 仍传「角色默认模型 > 激活配置 default_model」。原逻辑从 active 的 default_model 拿值 —— 现在 active 在 configs 数组里：`defaultModelName = activeConfig?.default_model ?? ''`。`defaultLabel` 字符串拼接保持不变（\"默认模型（<name>）\" / \"默认模型\"）。\n\n### 4. UI：两层 <select>\n- 第一个 select（**配置**）：仅在 configs.length > 0 时渲染，否则退化到只显示当前 model 下拉。\n  - value = selectedConfigId。\n  - 选项 = `<option value=\"\">{configs.length > 0 ? '默认（不指定）' : '当前生效（无配置可切换）'}</option>` + configs.map(c => `<option key={c.id} value={c.id}>{c.name}{c.is_active ? '（当前生效）' : ''}</option>)`。\n  - onChange = e => setSelectedConfigId(e.target.value)。\n  - title 文案：当 working 时仍禁用（与原 disabled prop 一致）；切换不强制清空 value（保留 __legacy 显示，让用户决定要不要改）。\n- 第二个 select（**模型**）：保持原有逻辑：\n  - value = outOfList ? `__legacy:${normalized}` : normalized（normalized = !value || value === DefaultModelLabel ? '' : value）。\n  - 选项 = `<option value=\"\">{defaultLabel}</option>` + 当前 config 的 models.map(m => `<option key={m} value={m}>{m}</option>)` + outOfList 时的 `__legacy:` disabled option。\n  - 当 selectedConfigId === '' 时（用户显式选了「默认（不指定）」），这个 select 仍可用，但只渲染「默认模型」一项 —— 不要再尝试从某 config 取 models（避免给用户错误印象）。\n  - 当 selectedConfigId 非空但 configs.find(c => c.id === id).models.length === 0，model select 仍渲染「默认模型」一项 + 一个 form-hint 「该配置未配置模型列表」。\n\n### 5. 状态指示器与样式\n- 保留 `working` 状态指示器（绿点+工作中 / 灰点+空闲）渲染逻辑 —— 它原本就是 ModelSelect 的一部分，三个调用方都用。位置放在「配置 select」前面（即视觉顺序：状态点 → 配置 → 模型）。\n- 保留 `label`、`style`、`disabled`、`title` props 行为。`disabled` 应用于**两个 select**，与原 ModelSelect 一致。\n- CSS：两层 select 视觉紧凑。给组件根加一个 className 比如 `model-select model-select-layered`，model-select-input 沿用现有 class。index.css 里如有 `.model-select` 样式无需大改 —— 给根加 `gap: 8px` 让两个 select 之间有间距、第二个 select 占满行。\n\n### 6. 不要破坏的细节\n- `outOfList` 判断保留：`normalized !== '' && cfgModels.length > 0 && !cfgModels.includes(normalized)`。\n- `DefaultModelLabel` 常量来自 api/client.ts（是「默认模型」字面值的 sentinel），继续沿用。\n- 失败兜底：claudeApi.list() 报错时 setConfigs([])，UI 退化为「只显示 defaultModelName + 单 select（空 models）」。\n\n## 验证\n- cd frontend && npm run lint 通过\n- 手工路径（可选但建议）：\n  1. 打开任意需求的详情页，进入「开始开发」弹窗，「开发模型」字段应看到两个 select；激活配置默认在第一个 select 中标注「（当前生效）」。\n  2. 切换配置后，模型下拉的 options 应联动。\n  3. 在第一个 select 中切到非激活配置，再切到激活配置，模型选项应恢复到激活配置的 models。\n  4. 选一个非激活配置的 model 并确认开始开发 —— 后端应正常接收（wizard handler 无 active 校验）。\n  5. analyst / architect 阶段同样应表现两层下拉（共用组件一次覆盖）。\n\n## 产物\n- 修改后的 frontend/src/components/ModelSelect.tsx\n- 修改后的 frontend/src/index.css（若加 .model-select-layered 样式）\n- npm run lint 输出成功"
  }
]}